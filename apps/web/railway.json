{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs_22"]
        },
        "install": {
          "dependsOn": ["setup"],
          "cmds": ["corepack enable", "corepack prepare pnpm@9.0.0 --activate", "cd ../.. && pnpm install --force"]
        },
        "build": {
          "dependsOn": ["install"],
          "cmds": ["cd ../.. && pnpm run build --filter=@lubia/web"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "cd ../.. && pnpm --filter @lubia/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
