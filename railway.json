{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs_23"]
        },
        "install": {
          "dependsOn": ["setup"],
          "cmds": ["corepack enable", "corepack prepare pnpm@9.0.0 --activate", "pnpm install --force"]
        },
        "build": {
          "dependsOn": ["install"],
          "cmds": ["pnpm turbo build --filter=@lubia/web"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "pnpm --filter @lubia/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
